#################### SQLite installation #####################
# 0. Update the source list
sudo apt update
# 1. Install SQLite
sudo apt install -y sqlite3

# 2. Check SQLite version
sudo sqlite3 --version